/**
 * Appointment feature — type re-exports.
 * All canonical types live in @/types/models; this barrel keeps feature
 * imports colocated.
 */
export type { Appointment, AppointmentStats } from '@/types/models/appointment';
