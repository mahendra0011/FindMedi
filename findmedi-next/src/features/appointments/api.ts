/**
 * Appointment feature — API wrappers.
 * Re-exports the typed endpoint groups so consumers import from one place.
 */
export { appointments, scheduleChangeRequests as scheduleChangeRequestApi } from '@/lib/api';
