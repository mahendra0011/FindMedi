{
  "name": "findmedi",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "npm run install:all",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "install:mindsupport": "npm install --prefix mindsupport",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev:full --prefix mindsupport\"",
    "dev:findmedi": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:mindsupport": "npm run dev:full --prefix mindsupport",
    "build": "npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "seed:admin": "npm run seed:admin --prefix backend",
    "seed:cluster": "npm run seed:cluster --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
