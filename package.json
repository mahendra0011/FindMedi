{
  "name": "findmedi",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "npm run install:all",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:findmedi": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:legacy": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev:mind --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:mind": "npm run dev:mind --prefix backend",
    "build": "npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "seed:admin": "npm run seed:admin --prefix backend",
    "seed:cluster": "npm run seed:cluster --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
