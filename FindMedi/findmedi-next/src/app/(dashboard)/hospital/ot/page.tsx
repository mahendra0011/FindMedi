/**
 * Operation Theatre — thin route wrapper (Todo 5 pattern).
 * UI lives in src/components/hospital/OtHub.tsx.
 */
'use client';

import { OtHub } from '@/components/hospital';

export default function OperationTheatrePage() {
  return <OtHub />;
}
