/**
 * Diet & Kitchen — thin route wrapper (Todo 5 pattern).
 * UI lives in src/components/patient/DietHub.tsx.
 */
'use client';

import { DietHub } from '@/components/patient';

export default function DietKitchenPage() {
  return <DietHub />;
}
