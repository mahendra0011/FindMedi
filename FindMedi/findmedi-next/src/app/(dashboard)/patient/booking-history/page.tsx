/**
 * Booking History — renders the shared booking history across all types.
 * Mirrors client/src/pages/patient/PatientBookingHistory.jsx.
 */
'use client';

import { PatientBookingHistory } from '@/components/patient';

export default function BookingHistoryPage() {
  return <PatientBookingHistory />;
}
